{
  "name": "webpagediff",
  "version": "0.1.0",
  "description": "Detect the difference between the current and the previous version in the webpage.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kazuaki Kadoya",
  "license": "ISC",
  "devDependencies": {
    "jsdom": "^11.0.0",
    "node-cron": "^1.1.3",
    "twitter": "^1.7.0"
  }
}
